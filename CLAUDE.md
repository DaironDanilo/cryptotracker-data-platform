@AGENTS.md

Everything above is shared across AI coding tools (the open `AGENTS.md`
standard) and kept there so it stays portable. This repo has no
Claude-Code-specific mechanism (no `.claude/skills`/`.claude/agents`/hooks)
yet, unlike the sibling `cryptoTracker` repo — this file exists mainly for
the `@AGENTS.md` import so Claude Code picks up the rules above
automatically. Add Claude-specific scaffolding here if it earns its keep,
matching `cryptoTracker/CLAUDE.md`'s pattern.
